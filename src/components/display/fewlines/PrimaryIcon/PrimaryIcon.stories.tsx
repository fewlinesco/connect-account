import React from "react";

import { PrimaryIcon } from "./PrimaryIcon";

export default { title: "PrimaryIcon", component: PrimaryIcon };

export const StandardPrimaryIcon = (): JSX.Element => {
  return <PrimaryIcon />;
};
