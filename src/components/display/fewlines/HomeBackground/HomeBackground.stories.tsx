import React from "react";

import { HomeBackground } from "./HomeBackground";

export default { title: "icons/HomeBackground", component: HomeBackground };

export const HomeBackgroundIcon = (): JSX.Element => {
  return <HomeBackground />;
};
