import React from "react";

import { LockIcon } from "./LockIcon";

export default {
  title: "icons/Lock Icon",
  component: LockIcon,
};

export const StandardLockIcon = (): JSX.Element => {
  return <LockIcon />;
};
