import React from "react";

import { BlackWorldIcon } from "./BlackWorldIcon";

export default {
  title: "icons/BlackWorldIcon",
  component: BlackWorldIcon,
};

export const StandardBlackWorldIcon = (): JSX.Element => {
  return <BlackWorldIcon />;
};
