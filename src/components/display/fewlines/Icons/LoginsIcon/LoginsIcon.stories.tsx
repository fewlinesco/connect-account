import React from "react";

import { LoginsIcon } from "./LoginsIcon";

export default { title: "icons/LoginsIcon", component: LoginsIcon };

export const StandardLoginsIcon = (): JSX.Element => {
  return <LoginsIcon />;
};
