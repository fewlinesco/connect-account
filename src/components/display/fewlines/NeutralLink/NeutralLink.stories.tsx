import React from "react";

import { NeutralLink } from "./NeutralLink";

export default { title: "components/Neutral Link", component: NeutralLink };

export const StandardNeutralLink = (): JSX.Element => {
  return <NeutralLink />;
};
