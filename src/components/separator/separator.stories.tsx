import React from "react";

import { Separator } from "./separator";

const StandardSeparator = (): JSX.Element => {
  return <Separator />;
};

export { StandardSeparator };
export default { title: "components/Separator", component: Separator };
