import React from "react";

import { LockIcon } from "./lock-icon";

const StandardLockIcon = (): JSX.Element => {
  return <LockIcon />;
};

export { StandardLockIcon };
export default {
  title: "icons/Lock Icon",
  component: LockIcon,
};
