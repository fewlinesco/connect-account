import React from "react";

import { ProfileIcon } from "./profile-icon";

const StandardProfileIcon = (): JSX.Element => {
  return <ProfileIcon />;
};

export { StandardProfileIcon };
export default { title: "icons/Profile Icon", component: ProfileIcon };
