import React from "react";

import { LoginsIcon } from "./logins-icon";

export default { title: "icons/Logins Icon", component: LoginsIcon };

export const StandardLoginsIcon = (): JSX.Element => {
  return <LoginsIcon />;
};
